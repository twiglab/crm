// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/twiglab/crm/game/orm/ent/predicate"
	"github.com/twiglab/crm/game/orm/ent/signintask"
)

// SignInTaskQuery is the builder for querying SignInTask entities.
type SignInTaskQuery struct {
	config
	ctx        *QueryContext
	order      []signintask.OrderOption
	inters     []Interceptor
	predicates []predicate.SignInTask
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SignInTaskQuery builder.
func (sitq *SignInTaskQuery) Where(ps ...predicate.SignInTask) *SignInTaskQuery {
	sitq.predicates = append(sitq.predicates, ps...)
	return sitq
}

// Limit the number of records to be returned by this query.
func (sitq *SignInTaskQuery) Limit(limit int) *SignInTaskQuery {
	sitq.ctx.Limit = &limit
	return sitq
}

// Offset to start from.
func (sitq *SignInTaskQuery) Offset(offset int) *SignInTaskQuery {
	sitq.ctx.Offset = &offset
	return sitq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sitq *SignInTaskQuery) Unique(unique bool) *SignInTaskQuery {
	sitq.ctx.Unique = &unique
	return sitq
}

// Order specifies how the records should be ordered.
func (sitq *SignInTaskQuery) Order(o ...signintask.OrderOption) *SignInTaskQuery {
	sitq.order = append(sitq.order, o...)
	return sitq
}

// First returns the first SignInTask entity from the query.
// Returns a *NotFoundError when no SignInTask was found.
func (sitq *SignInTaskQuery) First(ctx context.Context) (*SignInTask, error) {
	nodes, err := sitq.Limit(1).All(setContextOp(ctx, sitq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{signintask.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sitq *SignInTaskQuery) FirstX(ctx context.Context) *SignInTask {
	node, err := sitq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SignInTask ID from the query.
// Returns a *NotFoundError when no SignInTask ID was found.
func (sitq *SignInTaskQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sitq.Limit(1).IDs(setContextOp(ctx, sitq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{signintask.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sitq *SignInTaskQuery) FirstIDX(ctx context.Context) int {
	id, err := sitq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SignInTask entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SignInTask entity is found.
// Returns a *NotFoundError when no SignInTask entities are found.
func (sitq *SignInTaskQuery) Only(ctx context.Context) (*SignInTask, error) {
	nodes, err := sitq.Limit(2).All(setContextOp(ctx, sitq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{signintask.Label}
	default:
		return nil, &NotSingularError{signintask.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sitq *SignInTaskQuery) OnlyX(ctx context.Context) *SignInTask {
	node, err := sitq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SignInTask ID in the query.
// Returns a *NotSingularError when more than one SignInTask ID is found.
// Returns a *NotFoundError when no entities are found.
func (sitq *SignInTaskQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sitq.Limit(2).IDs(setContextOp(ctx, sitq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{signintask.Label}
	default:
		err = &NotSingularError{signintask.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sitq *SignInTaskQuery) OnlyIDX(ctx context.Context) int {
	id, err := sitq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SignInTasks.
func (sitq *SignInTaskQuery) All(ctx context.Context) ([]*SignInTask, error) {
	ctx = setContextOp(ctx, sitq.ctx, ent.OpQueryAll)
	if err := sitq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SignInTask, *SignInTaskQuery]()
	return withInterceptors[[]*SignInTask](ctx, sitq, qr, sitq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sitq *SignInTaskQuery) AllX(ctx context.Context) []*SignInTask {
	nodes, err := sitq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SignInTask IDs.
func (sitq *SignInTaskQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sitq.ctx.Unique == nil && sitq.path != nil {
		sitq.Unique(true)
	}
	ctx = setContextOp(ctx, sitq.ctx, ent.OpQueryIDs)
	if err = sitq.Select(signintask.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sitq *SignInTaskQuery) IDsX(ctx context.Context) []int {
	ids, err := sitq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sitq *SignInTaskQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sitq.ctx, ent.OpQueryCount)
	if err := sitq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sitq, querierCount[*SignInTaskQuery](), sitq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sitq *SignInTaskQuery) CountX(ctx context.Context) int {
	count, err := sitq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sitq *SignInTaskQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sitq.ctx, ent.OpQueryExist)
	switch _, err := sitq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sitq *SignInTaskQuery) ExistX(ctx context.Context) bool {
	exist, err := sitq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SignInTaskQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sitq *SignInTaskQuery) Clone() *SignInTaskQuery {
	if sitq == nil {
		return nil
	}
	return &SignInTaskQuery{
		config:     sitq.config,
		ctx:        sitq.ctx.Clone(),
		order:      append([]signintask.OrderOption{}, sitq.order...),
		inters:     append([]Interceptor{}, sitq.inters...),
		predicates: append([]predicate.SignInTask{}, sitq.predicates...),
		// clone intermediate query.
		sql:  sitq.sql.Clone(),
		path: sitq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TaskID int `json:"task_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SignInTask.Query().
//		GroupBy(signintask.FieldTaskID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sitq *SignInTaskQuery) GroupBy(field string, fields ...string) *SignInTaskGroupBy {
	sitq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SignInTaskGroupBy{build: sitq}
	grbuild.flds = &sitq.ctx.Fields
	grbuild.label = signintask.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TaskID int `json:"task_id,omitempty"`
//	}
//
//	client.SignInTask.Query().
//		Select(signintask.FieldTaskID).
//		Scan(ctx, &v)
func (sitq *SignInTaskQuery) Select(fields ...string) *SignInTaskSelect {
	sitq.ctx.Fields = append(sitq.ctx.Fields, fields...)
	sbuild := &SignInTaskSelect{SignInTaskQuery: sitq}
	sbuild.label = signintask.Label
	sbuild.flds, sbuild.scan = &sitq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SignInTaskSelect configured with the given aggregations.
func (sitq *SignInTaskQuery) Aggregate(fns ...AggregateFunc) *SignInTaskSelect {
	return sitq.Select().Aggregate(fns...)
}

func (sitq *SignInTaskQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sitq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sitq); err != nil {
				return err
			}
		}
	}
	for _, f := range sitq.ctx.Fields {
		if !signintask.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sitq.path != nil {
		prev, err := sitq.path(ctx)
		if err != nil {
			return err
		}
		sitq.sql = prev
	}
	return nil
}

func (sitq *SignInTaskQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SignInTask, error) {
	var (
		nodes = []*SignInTask{}
		_spec = sitq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SignInTask).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SignInTask{config: sitq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sitq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sitq *SignInTaskQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sitq.querySpec()
	_spec.Node.Columns = sitq.ctx.Fields
	if len(sitq.ctx.Fields) > 0 {
		_spec.Unique = sitq.ctx.Unique != nil && *sitq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sitq.driver, _spec)
}

func (sitq *SignInTaskQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(signintask.Table, signintask.Columns, sqlgraph.NewFieldSpec(signintask.FieldID, field.TypeInt))
	_spec.From = sitq.sql
	if unique := sitq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sitq.path != nil {
		_spec.Unique = true
	}
	if fields := sitq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, signintask.FieldID)
		for i := range fields {
			if fields[i] != signintask.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sitq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sitq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sitq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sitq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sitq *SignInTaskQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sitq.driver.Dialect())
	t1 := builder.Table(signintask.Table)
	columns := sitq.ctx.Fields
	if len(columns) == 0 {
		columns = signintask.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sitq.sql != nil {
		selector = sitq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sitq.ctx.Unique != nil && *sitq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sitq.predicates {
		p(selector)
	}
	for _, p := range sitq.order {
		p(selector)
	}
	if offset := sitq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sitq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SignInTaskGroupBy is the group-by builder for SignInTask entities.
type SignInTaskGroupBy struct {
	selector
	build *SignInTaskQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sitgb *SignInTaskGroupBy) Aggregate(fns ...AggregateFunc) *SignInTaskGroupBy {
	sitgb.fns = append(sitgb.fns, fns...)
	return sitgb
}

// Scan applies the selector query and scans the result into the given value.
func (sitgb *SignInTaskGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sitgb.build.ctx, ent.OpQueryGroupBy)
	if err := sitgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SignInTaskQuery, *SignInTaskGroupBy](ctx, sitgb.build, sitgb, sitgb.build.inters, v)
}

func (sitgb *SignInTaskGroupBy) sqlScan(ctx context.Context, root *SignInTaskQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sitgb.fns))
	for _, fn := range sitgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sitgb.flds)+len(sitgb.fns))
		for _, f := range *sitgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sitgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sitgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SignInTaskSelect is the builder for selecting fields of SignInTask entities.
type SignInTaskSelect struct {
	*SignInTaskQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sits *SignInTaskSelect) Aggregate(fns ...AggregateFunc) *SignInTaskSelect {
	sits.fns = append(sits.fns, fns...)
	return sits
}

// Scan applies the selector query and scans the result into the given value.
func (sits *SignInTaskSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sits.ctx, ent.OpQuerySelect)
	if err := sits.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SignInTaskQuery, *SignInTaskSelect](ctx, sits.SignInTaskQuery, sits, sits.inters, v)
}

func (sits *SignInTaskSelect) sqlScan(ctx context.Context, root *SignInTaskQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sits.fns))
	for _, fn := range sits.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sits.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sits.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
