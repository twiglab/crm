// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"github.com/twiglab/crm/game/orm/ent/migrate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"github.com/twiglab/crm/game/orm/ent/signintask"
	"github.com/twiglab/crm/game/orm/ent/usersignin"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// SignInTask is the client for interacting with the SignInTask builders.
	SignInTask *SignInTaskClient
	// UserSignIn is the client for interacting with the UserSignIn builders.
	UserSignIn *UserSignInClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.SignInTask = NewSignInTaskClient(c.config)
	c.UserSignIn = NewUserSignInClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:        ctx,
		config:     cfg,
		SignInTask: NewSignInTaskClient(cfg),
		UserSignIn: NewUserSignInClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:        ctx,
		config:     cfg,
		SignInTask: NewSignInTaskClient(cfg),
		UserSignIn: NewUserSignInClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		SignInTask.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.SignInTask.Use(hooks...)
	c.UserSignIn.Use(hooks...)
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	c.SignInTask.Intercept(interceptors...)
	c.UserSignIn.Intercept(interceptors...)
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *SignInTaskMutation:
		return c.SignInTask.mutate(ctx, m)
	case *UserSignInMutation:
		return c.UserSignIn.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// SignInTaskClient is a client for the SignInTask schema.
type SignInTaskClient struct {
	config
}

// NewSignInTaskClient returns a client for the SignInTask from the given config.
func NewSignInTaskClient(c config) *SignInTaskClient {
	return &SignInTaskClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `signintask.Hooks(f(g(h())))`.
func (c *SignInTaskClient) Use(hooks ...Hook) {
	c.hooks.SignInTask = append(c.hooks.SignInTask, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `signintask.Intercept(f(g(h())))`.
func (c *SignInTaskClient) Intercept(interceptors ...Interceptor) {
	c.inters.SignInTask = append(c.inters.SignInTask, interceptors...)
}

// Create returns a builder for creating a SignInTask entity.
func (c *SignInTaskClient) Create() *SignInTaskCreate {
	mutation := newSignInTaskMutation(c.config, OpCreate)
	return &SignInTaskCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SignInTask entities.
func (c *SignInTaskClient) CreateBulk(builders ...*SignInTaskCreate) *SignInTaskCreateBulk {
	return &SignInTaskCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SignInTaskClient) MapCreateBulk(slice any, setFunc func(*SignInTaskCreate, int)) *SignInTaskCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SignInTaskCreateBulk{err: fmt.Errorf("calling to SignInTaskClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SignInTaskCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SignInTaskCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SignInTask.
func (c *SignInTaskClient) Update() *SignInTaskUpdate {
	mutation := newSignInTaskMutation(c.config, OpUpdate)
	return &SignInTaskUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SignInTaskClient) UpdateOne(sit *SignInTask) *SignInTaskUpdateOne {
	mutation := newSignInTaskMutation(c.config, OpUpdateOne, withSignInTask(sit))
	return &SignInTaskUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SignInTaskClient) UpdateOneID(id int) *SignInTaskUpdateOne {
	mutation := newSignInTaskMutation(c.config, OpUpdateOne, withSignInTaskID(id))
	return &SignInTaskUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SignInTask.
func (c *SignInTaskClient) Delete() *SignInTaskDelete {
	mutation := newSignInTaskMutation(c.config, OpDelete)
	return &SignInTaskDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SignInTaskClient) DeleteOne(sit *SignInTask) *SignInTaskDeleteOne {
	return c.DeleteOneID(sit.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SignInTaskClient) DeleteOneID(id int) *SignInTaskDeleteOne {
	builder := c.Delete().Where(signintask.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SignInTaskDeleteOne{builder}
}

// Query returns a query builder for SignInTask.
func (c *SignInTaskClient) Query() *SignInTaskQuery {
	return &SignInTaskQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSignInTask},
		inters: c.Interceptors(),
	}
}

// Get returns a SignInTask entity by its id.
func (c *SignInTaskClient) Get(ctx context.Context, id int) (*SignInTask, error) {
	return c.Query().Where(signintask.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SignInTaskClient) GetX(ctx context.Context, id int) *SignInTask {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SignInTaskClient) Hooks() []Hook {
	return c.hooks.SignInTask
}

// Interceptors returns the client interceptors.
func (c *SignInTaskClient) Interceptors() []Interceptor {
	return c.inters.SignInTask
}

func (c *SignInTaskClient) mutate(ctx context.Context, m *SignInTaskMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SignInTaskCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SignInTaskUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SignInTaskUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SignInTaskDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SignInTask mutation op: %q", m.Op())
	}
}

// UserSignInClient is a client for the UserSignIn schema.
type UserSignInClient struct {
	config
}

// NewUserSignInClient returns a client for the UserSignIn from the given config.
func NewUserSignInClient(c config) *UserSignInClient {
	return &UserSignInClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `usersignin.Hooks(f(g(h())))`.
func (c *UserSignInClient) Use(hooks ...Hook) {
	c.hooks.UserSignIn = append(c.hooks.UserSignIn, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `usersignin.Intercept(f(g(h())))`.
func (c *UserSignInClient) Intercept(interceptors ...Interceptor) {
	c.inters.UserSignIn = append(c.inters.UserSignIn, interceptors...)
}

// Create returns a builder for creating a UserSignIn entity.
func (c *UserSignInClient) Create() *UserSignInCreate {
	mutation := newUserSignInMutation(c.config, OpCreate)
	return &UserSignInCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of UserSignIn entities.
func (c *UserSignInClient) CreateBulk(builders ...*UserSignInCreate) *UserSignInCreateBulk {
	return &UserSignInCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *UserSignInClient) MapCreateBulk(slice any, setFunc func(*UserSignInCreate, int)) *UserSignInCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &UserSignInCreateBulk{err: fmt.Errorf("calling to UserSignInClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*UserSignInCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &UserSignInCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for UserSignIn.
func (c *UserSignInClient) Update() *UserSignInUpdate {
	mutation := newUserSignInMutation(c.config, OpUpdate)
	return &UserSignInUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UserSignInClient) UpdateOne(usi *UserSignIn) *UserSignInUpdateOne {
	mutation := newUserSignInMutation(c.config, OpUpdateOne, withUserSignIn(usi))
	return &UserSignInUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UserSignInClient) UpdateOneID(id int) *UserSignInUpdateOne {
	mutation := newUserSignInMutation(c.config, OpUpdateOne, withUserSignInID(id))
	return &UserSignInUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for UserSignIn.
func (c *UserSignInClient) Delete() *UserSignInDelete {
	mutation := newUserSignInMutation(c.config, OpDelete)
	return &UserSignInDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UserSignInClient) DeleteOne(usi *UserSignIn) *UserSignInDeleteOne {
	return c.DeleteOneID(usi.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UserSignInClient) DeleteOneID(id int) *UserSignInDeleteOne {
	builder := c.Delete().Where(usersignin.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UserSignInDeleteOne{builder}
}

// Query returns a query builder for UserSignIn.
func (c *UserSignInClient) Query() *UserSignInQuery {
	return &UserSignInQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUserSignIn},
		inters: c.Interceptors(),
	}
}

// Get returns a UserSignIn entity by its id.
func (c *UserSignInClient) Get(ctx context.Context, id int) (*UserSignIn, error) {
	return c.Query().Where(usersignin.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UserSignInClient) GetX(ctx context.Context, id int) *UserSignIn {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UserSignInClient) Hooks() []Hook {
	return c.hooks.UserSignIn
}

// Interceptors returns the client interceptors.
func (c *UserSignInClient) Interceptors() []Interceptor {
	return c.inters.UserSignIn
}

func (c *UserSignInClient) mutate(ctx context.Context, m *UserSignInMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UserSignInCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UserSignInUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UserSignInUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UserSignInDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown UserSignIn mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		SignInTask, UserSignIn []ent.Hook
	}
	inters struct {
		SignInTask, UserSignIn []ent.Interceptor
	}
)
